class Product {
    constructor(productId, name, description, price, basePrice, filename) {
      this.productId = productId;
      this.name = name;
      this.description = description;
      this.price = price;
      this.basePrice = basePrice;
      this.filename = filename;
    }
  }
  
  export default Product;